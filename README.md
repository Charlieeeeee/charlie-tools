# charlie-tools

#### 自动添加版本
> npm version patch

#### 发布
> npm publish --access=public

#### 如果没有发布账户
> npm adduser

### tools内容
1. cookie :
    - setCookie(key,value)
    - getCookie(key)
    - deleteCookie(key)

